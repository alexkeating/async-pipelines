#!/bin/sh -e


set -x

python -m pip install -U pip
python -m pip install -U pipenv
python -m pipenv install -d --deploy
python -m pipenv install -e .

set +x

echo
echo "Success! You can now activate your virtual environment using:"
echo "pipenv shell"
